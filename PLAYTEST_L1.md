# Level_01 Playtest 方案（新手友好三件套验证）

> 状态：可执行。被测版本 = 新手友好三件套已落地（射程预览 + 漏怪安慰半额收入 + 首关开场引导）。
> 设计铁律：**没有失败定义的 playtest 等于没做**——下文 A/B/C 先定义"坏掉长什么样"，再开测。

---

## 1. 被测版本与背景

连续四轮已实现：UI 三档优化 → Phase A Juice → 战败闭环/可读性 → 新手友好。
全程**零一手玩家数据**，本方案专门补这一课。

三件套对应痛点（路线图标定的新手最劝退两点）：
- **建塔看不到射程** → `range_preview.gd` ghost 环（复用 Tower 射程环视觉）。
- **漏怪零补偿经济钉死 385** → `enemy.gd` 漏怪发半额晶币（`0.5` 标 `[PLACEHOLDER]`，待 Phase 1 校准）。
- **首关不知干嘛** → `level_01` 首次进入（`level_stars==0`）弹开场引导横幅（可关 / 9s 自动隐）。

## 2. Fun Hypothesis 复核

项目核心假设：**"用克制关系守住熔核的爽快摧毁感"**。
但 type-triangle 未实现，当前 level_01 实际好玩核心 = **布局建塔 + 看敌人被炸碎的 juice**。

本次 playtest 专验：**新手友好三件套是否让首次进入者独立撑过一关，而不被信息差/经济差劝退**。

## 3. 测试目标（对应三件套）

| # | 目标 | 验证点 |
|---|------|--------|
| T1 射程预览 | 玩家建塔前是否利用预览，而非闭眼建 | 建错位率、悬停切换率 |
| T2 漏怪安慰 | 漏 1–3 个时经济是否仍"够花" | 漏怪时晶币是否支撑继续建/升 |
| T3 首关引导 | wave 1 前是否理解目标、关闭时机 | 读了几秒、是否随后卡住 |

## 4. 客观遥测埋点（自动落盘，零人工记账）

`Telemetry` autoload（`scripts/autoload/telemetry.gd`）在每次结局（`SceneRouter.go_to_result`）自动写盘：

- 落盘路径：`user://telemetry/<level_id>_<时间戳>.json`
  - Windows 实际：`%APPDATA%\Godot\app_userdata\MOLTEN CORE 熔核守卫\telemetry\`
  - 不确定时点 Godot 控制台执行 `OS.get_user_data_dir()` 查。
- 采集事件：`build`(tower/saw_preview/hover_switches)、`wave_snapshot`(wave/leaked_total/currency)、`leak`(damage/core_hp)、`speed`、`pause`、`outcome`(victory/stars/reached_wave)；文件含 `duration_sec` 与 `meta`。
- **不联网、不弹窗**，写失败仅 `push_warning`，绝不崩（headless 冒烟安全）。

## 5. 失败信号 A / B / C

> 阈值全部标 `[PLACEHOLDER]`——无任何基线数据，不能假装这些数是算出来的。首轮 n=3 跑完后回填。

| 档 | 信号 | 触发即 |
|----|------|--------|
| **A 致命** | A1 无 TD 经验者 wave 3 前因"不知道干嘛/建不起塔"退出，且说不清目标 | 阻断发布 |
| | A2 核心 loop 出现 >3s"无事可做"空窗，主观评"无聊" | 阻断发布 |
| | A3 首关通关率 < 40% `[PLACEHOLDER·阈值待首轮校准]` | 阻断发布 |
| **B 重要** | B1 射程预览使用率 < 30% `[PLACEHOLDER]`，或看了仍建无效位 | 需调参 |
| | B2 漏 2–3 个仍崩（0.5 不够）/ 或经济溢出无挑战（0.5 过多） | 需调参 |
| | B3 引导被秒关（<2s `[PLACEHOLDER]`）且随后卡住 | 需调参 |
| **C 轻微** | C1 射程环在亮色地面不够清晰 / C2 引导文案偏长 / C3 波间信息密度过高 | 观察记录 |

## 6. 主观观察清单（会话中记）

- **建塔决策**：第几座塔在看了预览后建？有没有建在射程完全覆盖不到路径的格子？
- **经济行为**：第一次漏怪时的晶币数；是继续建/升，还是停摆？
- **情绪点**：哪个瞬间"爽"（击杀/juice）、哪个瞬间"懵"（来袭/漏怪）？
- **引导**：读了几秒？点关闭还是等 9s 自动消失？

## 7. 会话流程

1. 给**无 TD 经验**的休闲玩家（mobile 优先），只说"守住中央熔核"，不讲玩法。
2. 实玩一局（~8 分钟单局 + 15 分钟引导访谈）。
3. 事后 3 问：① 你以为目标是什么？② 最懵/最爽的瞬间？③ 漏怪时觉得还有希望吗？
4. 取证：回收 `user://telemetry/` 下该局的 json。
5. 对照 A/B/C 判 **PASS / 修 B / 修 A**。

## 8. 数据回收与判定

- 样本 ≥ 3 人（n 小，定性为主，不定量下结论）。
- 用 `level_01` 首进（`stars==0`）触发引导；重测前清存档或换号。
- 遥测 json 聚合：画经济爬升曲线（currency vs wave，标漏怪点），统计 `saw_preview` 命中率。
- 一手感受（清单）+ 客观曲线（遥测）一起回来，再定漏怪 `0.5` 与 `385` 上限——把上一轮的 `[PLACEHOLDER]` 坐实。

## 9. 张力摊开（不假装一致）

- **真人 playtest 才能测"好玩"，但 n=3 只能定性**；遥测给客观曲线，却不测"感受"。→ 双轨：3 人主观 + 遥测埋点。
- **漏怪安慰收入是条没有对应 sink 的新 source**——单关短局无害，长期抬通胀上限。比例 `0.5` 是拍脑袋的 `[PLACEHOLDER]`，Phase 1 必须按真实漏怪分布校准。这是"新手共情 vs 经济理性"的典型打架：先给新手喘口气，数值留待校准。

# MOLTEN CORE 熔核守卫 · 设计路线图 (Design Roadmap)

> 作者视角：做了十几年游戏设计的系统/机制设计师。本文档是**战略层路线图**，不是最终数值表。
> 所有数值分三类标注：**有 rationale**（已论证）/ **[PLACEHOLDER]**（待 playtest 验证）/ **待验证**（需 Monte Carlo / 实测）。
> 红线：不写没有 rationale 的数值；不跳过 paper prototype 直接实现；不承诺"会火"。

---

## 0. 设计地基 (Foundation)

**Fun Hypothesis（一句话）**：这游戏好玩的核心是——*在 8 分钟里，用越来越聪明的塔阵把涌来的敌人炸成一片片爽快碎片，且每一波都能提前看到"谁来、怎么死"。*

**Design Pillars（不可妥协的体验标准，每条决策过审）**：
1. **爽快感优先于拟真**：hit-stop、爆裂、屏震、飘字是底线体验，不能因"性能"整体砍掉（只能按 reduce_motion 降级）。
2. **决策可见**：玩家在开打前必须知道下一波构成（出兵预览已落地），战败后必须知道输在哪（漏怪数已落地）。
3. **8 分钟一局的紧凑节奏**：单局不能拖过 12 分钟，也不能短于 4 分钟——拖沓和潦草都是失败。
4. **休闲友好的认知负荷**：mobile 单手/双手操作，关键控件 ≥48px，机制认知不超过"3 种塔 + 1 个熔核技能栏"。
5. **失败是回流不是劝退**：战败面板给"第 N 波 + 漏 N 个"锚点，下一步要能直接重试本波。

**已拍板基础事实（用户 2026-08-07 确认）**：
- 品类 = 塔防 (TD)
- 平台 = mobile 为主，Windows 为辅
- 单局时长 ≈ 8 分钟
- 目标用户 = 休闲手游玩家
- Fun = 爽快感

---

## 1. 当前进度快照 (As-Built，截至 2026-08-07)

| 系统 | 状态 | 备注 |
|------|------|------|
| Core Loop（波次→建造→索敌→击杀→结算） | ✅ 已跑通 | 5 关数据齐，无头冒烟零错误 |
| Juice 层（hit-stop / 击杀爆裂 / 屏震 / 飘字 / 枪口闪） | ✅ 已落地 | Boss 死亡加长顿帧+屏震（本轮增强） |
| 出兵预览（下一波构成聚合显示） | ✅ 本轮新增 | `level._build_wave_preview` → `hud.show_next_wave_countdown(preview)` |
| 波次清空庆祝（"第 N 波 肃清！"横幅+轻屏震） | ✅ 本轮新增 | 依赖新增 `spawn_manager.wave_spawn_completed` 信号 |
| 战败漏怪归因（"漏过 N 个敌人"） | ✅ 本轮新增 | 依赖 `core.core_damaged` 计数，护盾免伤不计入 |
| 统一转场 / 集中调色板 / mobile 48px 触控 | ✅ 已落地 | ThemeConstants 集中调色板 + 安全区适配 |
| **经济与平衡模型** | ❌ 未建模 | 见 Phase 1，最高优先 |
| **克制三角（enemy_type / armor_pierce）** | ❌ 未做 | 见 Phase 2，数据缺口已确认 |
| 塔射程/溅射预览（建造时显示覆盖） | ❌ 未做 | Phase 3 TODO |
| 单局时长实测 | ❌ 未实测 | Phase 0 验证项，当前假设 8min |

---

## 2. Phase 0 — 核心循环与单局节奏验证（最高优先，先做）

**目的**：确认"8 分钟单局"在真实操作下成立，否则后续所有进阶曲线都是建在沙子上。

**验证路径（playtest 前先定义失败信号）**：
- **A 信号（节奏坏掉）**：实测单局 < 4min 或 > 12min → 整条进阶/经济曲线重画。
- **B 信号（循环空洞）**：核心能量全程从未低于 50% 就通关 → 难度过低，无张力。
- **C 信号（塔阵同质）**：某一塔整局 0 击杀，或某一塔击杀占比 > 70% → 塔差异没拉开。

**做法**：paper prototype 先估，再上真机计时跑 5~10 局录屏，取 p50/p90 时长。
**当前假设**：平均单局 ~8min。*若实测是 12min，整条 Phase 1 经济节奏与 Phase 4 进阶曲线要重画。*

---

## 3. Phase 1 — 经济与平衡建模（P0 级，首发前必须闭环）

**核心纪律**：任何一个 currency source 找不到对应 sink，通胀只是时间问题。当前是**单线性货币（晶币）**，只有一个 sink 族（建塔/升级/熔核技能）。必须先画清图。

### 3.1 Sources & Sinks（以 level_01 实测数据为例，已读 .tres）

**Sources（晶币来源）** —— 仅来自击杀奖励（`enemy._die_and_reward` 内 `add_currency`）：
| 波次 | 构成 | 晶币 |
|------|------|------|
| 起始 | starting_currency | **120** |
| W1 | 5×灼奴(5) | 25 |
| W2 | 6×灼奴(5) | 30 |
| W3 | 6×灼奴(5)+2×熔壳兵(12)+1×熔岩巨像(80) | 134 |
| W4 | 8×灼奴(5)+3×熔壳兵(12) | 76 |
| **完美通关总收入** | | **385** |

> ⚠️ **关键发现**：敌人突破熔核走 `die_silently()`，**不发放晶币**。所以总收入被钉死在 385，每一次漏怪都悄悄抽走玩家预算。配合 `core_max_energy=20`，经济极紧——休闲 mobile 可能过苦，需 Phase 1 实测决定上调起始/奖励还是下调塔价。

**Sinks（晶币去向）**：
| 去向 | 数值（实测） | rationale |
|------|------|------|
| 熔火塔 cost | 50 | [PLACEHOLDER · 需对比 DPS/单局预算] |
| 霆电塔 cost | 80 | [PLACEHOLDER] |
| 熔晶塔 cost | 60 | [PLACEHOLDER] |
| 熔核炮 cost | 90 | [PLACEHOLDER] |
| 升级 base | 30/50/40/50（按塔） | [PLACEHOLDER · 递增曲线未建模] |
| 熔核技能 | heal 30 / shock 50 / shield 80 | [PLACEHOLDER · core.gd 硬编码] |
| 出售返还 | `get_sell_value()` | [PLACEHOLDER · 公式未读] |

**典型预算推演**：2×熔火(100)+1×霆电(80)+1×熔晶(60)=240 建塔；2~3 次升级≈90；1~2 次熔核技能≈110 → 合计 ≈440 > 可用 385。
**结论**：在 zero-leak 假设下预算已偏紧，任何漏怪都会缺口扩大 → **Phase 1 必须跑 Monte Carlo 找可承受缺口**。

### 3.2 通胀/紧缩检查清单
- [ ] 每波 reward 增量 vs 该波后建议 build 增量是否匹配（不能后期随便建）
- [ ] 升级 cost 是否随等级**递增**（当前 `base_upgrade_cost` 固定，是通胀隐患 → 需改递增公式 [PLACEHOLDER]）
- [ ] 熔核技能 cost 是否构成有效 sink（当前 30/50/80 对 385 总盘占比合理，但需跨 5 关验证）

### 3.3 验证手段
- **Monte Carlo**：跑 1000 局随机 build 组合，输出通关率分布，找破局点（某塔过强/过弱、某波漏怪率 >40%）。
- **tuning spreadsheet 与 design doc 同步建**（红线：事后补表 = 没做）。
- **失败信号**：同一 build 1000 局胜率 >95%（过易）或 <30%（过难）→ 回炉。

---

## 4. Phase 2 — 决策空间：克制三角（Type Triangle）

**数据缺口（已确认）**：
- `EnemyData` 有 `armor` 但**无 `enemy_type`**
- `TowerData` 有伤害/范围/溅射/减速但**无 `armor_pierce` / `damage_tag`**

**后果**：当前塔差异只是数值（单体/范围/减速），无"按波次构成选塔"的真实决策。塔防退化为"哪塔数值高建哪塔"。

**设计提案**：
- 加 `enemy_type ∈ {轻甲/重甲/群聚/高速}`（或极简 2 类：轻甲/重甲）。
- 加 `tower.armor_pierce` 与 `tower.damage_tag`，让"熔核炮克重甲群聚、霆电克轻甲、熔晶控场"成立。
- **可视化抓手已就位**：出兵预览显示敌人 `display_name`，下一步把类型图标并排显示即可，零额外认知跳跃。

**风险（玩家共情 vs 系统理性）**：克制三角增加决策深度，但也增加休闲玩家认知负担。8 分钟 mobile 不能塞满机制。
**优先级**：留存期关键，但**首发可先靠 3~4 塔数值差异化撑住**，克制三角放 P2 中期扩内容时引入。

---

## 5. Phase 3 — 可读性与反馈闭环

**已完成（本轮 + 前期）**：出兵预览、波次肃清庆祝、战败漏怪归因、统一转场、集中调色板、mobile 48px 触控、reduce_motion 降级开关。

**TODO（按性价比排序）**：
1. **塔射程/溅射预览**：建造/选中时显示覆盖范围环（现有 `tower.set_range_visible` 已支持单体范围，需扩到溅射半径 + 升级后预览）。
2. **敌人类型图标常驻**：血条旁显示类型标签，配合 Phase 2 克制三角。
3. **技能 tooltip**：当前熔核技能/塔技能只有文字按钮，缺"点了会发生什么"的悬浮说明。

---

## 6. Phase 4 — 重玩与留存

| 项 | 状态 | 计划 |
|----|------|------|
| 关卡进阶曲线 | 5 关已建 | 基于 Phase 1 经济模型 tune 难度爬升 |
| 星级系统 | 3 星阈值已设（core_energy 50%/80%） | 实测可达且有挑战，避免"随便 3 星" |
| 剧情日志 | 已建 | 与关卡解锁挂钩，需扩写 |
| 战败回流 | 已给"第 N 波+漏 N 个" | 下一步加"重试本波"或"推荐塔组"降低挫败 |
| 元进度（塔解锁/皮肤/每日挑战） | 未做 | **暂缓**：核心循环验证好玩后再议，避免早 optimising 留存 |

---

## 7. Phase 5 — 内容工艺与平台打磨

- **美术**：当前方块粒子 + emoji + 程序化，风格统一但粗糙。mobile 需高对比、大色块、弱细节。
- **音效**：SFX 已挂（enemy_kill/boom/critical_hit/core_damaged 等），需混音与 mobile 外放响度优化。
- **Boss 设计**：2 Boss 已建（熔岩巨像 / 熔心君主），phase 节奏需 tune（当前 phase 按血线触发）。
- **移动端真机**：横屏已设；需真机手势/刘海/性能（draw call、粒子数）回归，确认 8 分钟不烫不卡。

---

## 8. 执行顺序与里程碑

```
Phase 0  单局节奏实测 ──► 确认 8min 成立（否则重画）
   │
Phase 1  经济/平衡建模 ──► sources/sinks 图 + Monte Carlo + tuning 表（首发前必须）
   │
Phase 2  克制三角 ──► enemy_type + armor_pierce（留存期，中期）
   │
Phase 3  可读性补完 ──► 射程预览 / 类型图标 / tooltip（穿插做）
   │
Phase 4  重玩留存 ──► 进阶曲线 tune / 战败回流 / 剧情
   │
Phase 5  工艺打磨 ──► 美术/音效/Boss/真机
```

**为什么这个顺序**：Phase 0/1 是地基——节奏不对、经济不平，后面所有内容都是浪费。Phase 2 是"好玩变耐玩"的杠杆，但首发布局不阻塞。Phase 3 的部分项（可读性）已前置做完，因为零资产、高性价比、直接服务爽感与回流。

---

## 9. 风险与张力（摊开讲，不假装一致）

- **玩家共情 vs 系统理性**：出兵预览/漏怪数来自共情（玩家需要知道发生了什么），但经济紧绷来自理性（通胀恐惧）。两者可能在"休闲友好"上打架——用 Phase 1 Monte Carlo 找平衡点。
- **专注核心 vs 防通胀**：核心循环本身得好玩，但任何 source 没 sink 就是定时炸弹。当前升级 cost 固定是隐患，先改递增。
- **纸面先验证 vs 数据再调**：paper prototype 先把 fun hypothesis 跑通，但"上线后再调"等于不去调——Phase 0/1 的 playtest 必须在首发前完成。

---

## 10. 待办速查（本轮代码改动落地清单）

- `spawn_manager.gd`：新增 `wave_spawn_completed(wave_index)` 信号（波次生成完毕）
- `level.gd`：漏怪计数 `_leaked` + 每波存活 `_wave_alive` + 清空庆祝 `_try_celebrate` + 出兵预览聚合 `_build_wave_preview` + `go_to_result` 传 `leaked`
- `core.gd`：复用既有 `core_damaged(amount, current)` 信号（无需改动）
- `scene_router.gd`：`go_to_result` 增 `leaked` 参数入 `pending_result`
- `result_screen.gd`：战败面板补"漏过 N 个敌人"
- `hud.gd`：`show_next_wave_countdown(preview)` 做出兵预览 + `show_wave_cleared` 肃清横幅
- `enemy.gd`：Boss 死亡重击（`HitStop.trigger(0.12)` + `trauma(0.6)`）
- 无头冒烟：SMOKE_TEST_OK / SMOKE_RESOURCES_OK / SMOKE_DONE，零脚本错误
